services:
  henri:
    build:
      context: .
      network: host
    network_mode: host
    env_file: .env
    volumes:
      - ./data:/app/data
    read_only: true
    tmpfs:
      - /tmp:size=100M
    security_opt:
      - no-new-privileges:true
    command: ["run_all"]
