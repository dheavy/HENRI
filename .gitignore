data/raw
data/processed
data/reports
data/grafana_dashboards
.env
data/.acled_token.json
data/.pipeline.lock
data/henri.db
.venv/
__pycache__/

.DS_Store

.claude
.mcp.json

# Khoregos: .khoregos/ is versioned intentionally — do not ignore it.
# Runtime artifacts inside it are excluded by .khoregos/.gitignore.
